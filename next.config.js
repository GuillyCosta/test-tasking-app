/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // Pode adicionar outras configurações do Next.js aqui
};

export default nextConfig; // Se for .mjs
// module.exports = nextConfig; // Se for .js